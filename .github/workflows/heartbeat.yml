name: sovereign-heartbeat

on:
  schedule:
    - cron: "0 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Streamlit app
        run: curl -fsSL "https://makemoneywithtommy.streamlit.app/?headless=1" >/dev/null
