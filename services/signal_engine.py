from __future__ import annotations


class SignalEngine:
    """Placeholder signal service boundary for future extraction."""

    version = "scaffold-v1"

    def describe(self) -> dict[str, str]:
        return {
            "service": "signal_engine",
            "status": "placeholder",
            "purpose": "Future reusable signal composition layer for UI and API consumers.",
        }

